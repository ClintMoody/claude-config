Run the Claude Code API usage history report. Execute this command and display the output directly:

```
python3 ~/.claude/usage-history.py --by-model
```

Show the full output to the user with no additional commentary.
